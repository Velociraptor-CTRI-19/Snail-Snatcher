const path = require('path');
import React from 'react';
import '@testing-library/jest-dom';
import { render, screen } from '@testing-library/react';
import Buyers from '../client/components/Buyers';


describe('Buyers', () => {

    let items = [item]

//we would expext the unordered list to be empty 
test('the button should remove item from items', () => {

    //we are asking when we effet button, does the items length decreement by 1
    

  });






})
